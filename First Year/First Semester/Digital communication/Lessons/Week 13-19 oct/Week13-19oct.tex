\documentclass[a4paper, 12pt]{book}
\input{../../common_preamble}
\usepackage[x11names]{xcolor}
\usepackage{tikz}
\usetikzlibrary{shapes,fit}
\begin{document}
\chapter[Week 13-19 Oct]{Week 13-19 October}

\section{Introduction}
In most of the course we'll handle wireless communications, as they're much more difficult than wired communications. In this course we'll stay at the physical layer.
We'll see:
\begin{itemize}
    \item \textbf{Channel models}: taking radio communications and making a mathematical model for them. We'll find an accurate model and then we'll apply a tradeoff to it, simplifying it so that it is manageable.
    \item \textbf{OFDM}: Orthogonal Frequency Division Multiplexing.
    \item \textbf{MIMO}: we have multiple antennas at the transmitter and multiple antennas at the receiver; we'd like to see how much this improves communications. The book he selected this year handles fundamentals of MIMO communications. MIMO is the latest and greatest achievement, he chose a book which goes from the basics. We might have single user MIMO or even multiple user MIMO: we're communicating to multiple phones through multiple antennas.
\end{itemize}

We can ask ourselves: what is the best that we can do? This is what our approach will be, so that we can know the boundaries.
We'll follow the slides from the author of the book, hopefully they'll be good.
We'll also have exercises with solutions: we can look at them from \href{https://cambridge.org}{cambridge.org}, specifically from the page of the book.

\section{Primer}
The first chapter is a primer in information theory and MSE estimation; we'll go fast as it should summarize stuff we know, as mutual information between two variables.

\begin{purplebox}[Note]
    The signals are always complex-valued and the constellations are always normalized to have unit power.
    $\infty$-QAM: we increase the number of points making them denser inside a specific set.
\end{purplebox}

\subsection{Complex symmetric random variables, \texorpdfstring{$\infty$}{infinity}-modulations}
We'll start from chapter 1 of the book: \textit{A primer on information theory and MSE}.

We should already know this stuff, but we'll cover it in order to have a common starting ground.

\textit{*Prof's backstory moment*}: In the end, he studies \textbf{physical layer} and that's it.

We ask ourselves: ``\textbf{what is information}? When can we say that someone is giving us information?''.
\textit{*Professor begins ranting about information*}: information is related to \textbf{probability}! The more unlikely something is, the more informative it is!

Today we'll talk about \textbf{mutual information} between two random variables: the information a variable can supply to another variable.

We'll talk about \textbf{channel capacity}: the capacity at which a channel can convey information; it answers to ``how much information can I send through this channel, reliably?''. By reliably we mean that we can lower the probability of error as much as we want.

\textbf{Binary coding} and \textbf{decoding} are near optimum, but there can be other ways, which could be better: we'll show that binary is indeed \textbf{optimum}.

\textbf{LMMSE} is optimum when the signal and noise are \textbf{Gaussian}.

Let's start from \textbf{signals}; in the book, signals are normally \textbf{complex}, unless we specifically say otherwise.
Normally \textbf{complex random variables} are \textbf{complexly symmetric}:

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[scale=1.5]
        % Assi Re (orizzontale) e Im (verticale)
        \draw[->, very thick] (-1.5, 0) -- (1.5, 0) node[right] {Re};
        \draw[->, very thick] (0, -1.5) -- (0, 1.5) node[above] {Im};
        
        % Cerchio centrale all'origine (PFD della variabile)
        \draw[thick] (0, 0) circle (0.5);
        
        % Freccia di rotazione (arco curvo)
        \draw[->, very thick] (0.7, 0.1) arc (10:80:0.7);
        
        % Annotazione dell'origine
        \draw[thick] (0, 0.05) -- (0, -0.05);
        \draw[thick] (0.05, 0) -- (-0.05, 0);
    \end{tikzpicture}
    \caption{Rappresentazione del piano Im-Re con una PFD simmetrica centrale e freccia di rotazione.}
\end{figure}

About \textbf{M-PSK} and \textbf{M-QAM}: $M$ is usually a \textbf{power of 2}. Let's see \textbf{16-QAM}:

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[scale=0.8]
        \draw[->] (-2.5, 0) -- (2.5, 0);
        \draw[->] (0, -2.5) -- (0, 2.5);

        \def\a{0.5} % Distanza primo livello
        \def\b{1.5} % Distanza secondo livello

        % Tutti i 16 punti QAM
        \foreach \x in {-\b, -\a, \a, \b} {
            \foreach \y in {-\b, -\a, \a, \b} {
                \filldraw[black] (\x, \y) circle (2pt);
            }
        }
        
        % Riquadro Blu (punti a potenza minore nell'immagine originale)
        \draw[blue, thick] (-\a-0.25, -\a-0.25) rectangle (\a+0.25, \a+0.25);
        \foreach \x in {-\a, \a} {
            \foreach \y in {-\a, \a} {
                \filldraw[blue] (\x, \y) circle (2pt);
            }
        }
    \end{tikzpicture}
    \caption*{The power of the ones in blue is lower than the power of the other signals.}
\end{figure}

In the book they always keep the \textbf{same variance of the constellation}: the book squeezes the values so that the \textbf{power stays constant}.

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[scale=0.8]
        \draw[->] (-2.5, 0) -- (2.5, 0);
        \draw[->] (0, -2.5) -- (0, 2.5);
        
        \def\a{0.2}
        \def\b{0.6}
        
        \foreach \x in {-\b, -\a, \a, \b} {
            \foreach \y in {-\b, -\a, \a, \b} {
                \filldraw[gray] (\x, \y) circle (1pt);
            }
        }
    \end{tikzpicture}
\end{figure}

The book also has \textbf{inf-PSK} and \textbf{inf-QAM}: We take the \textbf{normal constellation} and we keep \textbf{adding points}.

\begin{figure}[h]
    \centering
    \begin{tikzpicture}[scale=0.8]
        \draw[->] (-2.5, 0) -- (2.5, 0);
        \draw[->] (0, -2.5) -- (0, 2.5);
        \draw[thick] (0, 0) circle (2);
        \node at (0, -3) {$\mathbf{\infty - PSK}$};
        % Punti sulla circonferenza (simulazione)
        \foreach \angle in {0, 10, ..., 350} {
            \filldraw[black] (\angle:2) circle (1pt);
        }
    \end{tikzpicture}
\end{figure}

It's a useful way to see what happens in the \textbf{asymptotic regime}.
Also, the \textbf{PDF of a complex Gaussian variable exists} (see notes).

If $\sigma^2$ is the variance, the \textit{Standard deviation} is $\sqrt{\sigma^2} = \sigma$.
\begin{center}
    \textbf{\underline{NOT}} $\pm\sigma^2!$
\end{center}

We'll try and be as precise as possible: there is no \textbf{negative standard deviation}!
As an example:
\begin{align*}
    & f(x) = \sqrt{x} \in \mathbb{R} \text{ is def. only for } x \geq 0 \\
    & x^2 = 16 \text{ has two sol.}
\end{align*}

What about information? There have been many results, dating back to \textbf{Shannon}! (See pag 7 of the slides btw).

\section{Discrete entropy, proper signals}

\subsection{Entropy}

\begin{definition}[Entropy of a discrete-time random variable]
\begin{align*}
    \mathcal{H}(x) &= -\sum_{x} p_x(x) \log_2 p_x(x) \\
                   &= -\mathbb{E}[\log_2 p_x(x)]
\end{align*}
\end{definition}

\begin{definition}[Joint entropy]
\begin{align*}
    \mathcal{H}(x_0, x_1) &= -\sum_{x_0}\sum_{x_1} p_{x_0 x_1}(x_0, x_1) \log_2 p_{x_0 x_1}(x_0, x_1) \\
                          &= -\mathbb{E}[\log_2 p_{x_0 x_1}(x_0, x_1)]
\end{align*}
\end{definition}

\begin{greenbar}
    \textbf{Nonnegative function that quantifies the amount of uncertainty in discrete valued RV x}
\end{greenbar}

\textbf{Conditional entropy}
\begin{equation*}
    \mathcal{H}(x|y) = -\sum_{x}\sum_{y} p_{xy}(x,y) \log_2 p_{x|y}(x|y).
\end{equation*}

\textbf{Entropy connections}
\begin{equation*}
    \mathcal{H}(x,y) = \mathcal{H}(x) + \mathcal{H}(y|x).
\end{equation*}

The entropy is used in statistical physics.

\begin{purplebox}[Differential information]
    $\triangle$ Diff. inform. \underline{can} be negative.
\end{purplebox}

From this, we can use the Bayes theorem to derive:
\begin{align*}
    H(x,y) &= H(x|y) + H(y) \\
           &= H(y|x) + H(x)
\end{align*}

\begin{example}[Bernoulli Variable]
    We have a Bernoulli variable:
    \[ X = \{ 0, 1 \} \qquad P = \{ p, 1-p \} \]
    $H(X)$ will be $\leq 1$, where the equality holds if $p=1/2$.
    
    If we plot it, we get:
    \begin{center}
    \begin{tikzpicture}[scale=1.5]
        % Assi
        \draw[->] (-0.2,0) -- (1.5,0) node[right] {$p$};
        \draw[->] (0,-0.2) -- (0,1.5) node[above] {$H(x)$};
        
        % Ticks
        \draw (0.5, 0.05) -- (0.5, -0.05) node[below] {$\frac{1}{2}$};
        \draw (1.0, 0.05) -- (1.0, -0.05) node[below] {$1$};
        
        % Curva Entropia
        \draw[thick, blue] plot[domain=0.001:0.999, samples=100] (\x, { -(\x*log2(\x) + (1-\x)*log2(1-\x)) });
        
        % Linea tratteggiata per il massimo
        \draw[dashed] (0.5,0) -- (0.5,1);
    \end{tikzpicture}
    \end{center}
    
    We get maximum information when we flip a \textbf{*fair*} coin!
    If the coin always flips heads or tails, we get no information!
\end{example}

We could say that the messier the information, the most we get out of it.

Prof forgot stuff about signals: we're always talking about ``proper'' signals; the real and imaginary part of those numbers are uncorrelated!

Properness is preserved by affine transformations. An intuitive reason why we consider only proper complex numbers is that if we have real and imaginary parts of a random variable:

\begin{center}
\begin{tikzpicture}[scale=1.2]
    % Assi
    \draw[->] (-1.5,0) -- (1.5,0) node[right] {$Re$};
    \draw[->] (0,-1.5) -- (0,1.5) node[above] {$Ir$};
    
    % Punti Reali (Neri)
    \filldraw[black] (-1,0) circle (2pt) node[below] {$(-1,0)$};
    \filldraw[black] (1,0) circle (2pt) node[below] {$(1,0)$};
    
    % Punti "Impropri" (Blu)
    \filldraw[cyan] (1,1) circle (2pt) node[above right] {$(1,1)$};
    \filldraw[cyan] (-1,-1) circle (2pt) node[below left] {$(-1,-1)$}; 
\end{tikzpicture}
\end{center}

$(-1, -1)$: \textbf{This is a fake complex variable; we want to avoid these cases.}

\subsection{Proper complex}

A random variable (or a random process/random vector) is said to be proper complex if it satisfies \textbf{Properness}:

\begin{definition}[Properness]
    A complex random variable is said to be \textbf{proper} if
    \[ E[x^2] = E[x]^2 \]
    holds. Properness requires $\mathrm{Re}[x], \mathrm{Im}[x]$ to be uncorrelated and have the same variance.

    A complex random vector is said to be \textbf{proper} if
    \[ E[\bm{x}\bm{x}^T] = E[\bm{x}]E[\bm{x}]^T \]
    holds, which is equal to asking that $\mathrm{Re}[\bm{x}], \mathrm{Im}[\bm{x}]$ have identical Covariance matrices and cross-covariance equal to 0.
\end{definition}

\begin{remark}
    Properness is preserved in affine transformations!
\end{remark}

\subsection{Entropy rate}

Let's say we have a sequence of discrete random variables indexed by time, $x_0, \dots, x_{N-1}$.
If they are \textbf{IID}, then the entropy of the whole process grows linearly with $N$ at a rate $\mathcal{H}(x_0)$.

\textbf{In general}, a process grows according to the so-called \textit{entropy rate}
\begin{equation}
    \mathcal{H} = \lim_{N \to +\infty} \frac{1}{N} \mathcal{H}(x_0, \dots, x_{N-1}). \tag{1.32}
\end{equation}

If the process is \textbf{stationary}, we can show that the entropy rate is equal to
\begin{equation}
    \mathcal{H} = \lim_{N \to +\infty} \mathcal{H}(x_N | x_0, \dots, x_{N-1}). \tag{1.33}
\end{equation}

All of this can be extended for continuous processes to differential entropy.

A process is said to be \textbf{nonregular} if its present value is perfectly predictable from noiseless observations of the entire past, while the process is \textbf{regular} (which happens if $\mathfrak{h} > -\infty$) if its present value cannot be perfectly predicted from noiseless observations of the entire past.

\subsection{Differential entropy}

\begin{definition}[Differential entropy]
    \[ h(x) = \int f_x(x) \log_{1/2} f_x(x) \, dx = \mathbb{E}\left[\log_{1/2} f_x(x)\right] \]
    \textit{For vectors:}
    \[ h(\underline{x}) = \mathbb{E}\left[\log_{1/2} f_{\underline{x}}(\underline{x})\right] \]
\end{definition}

The differential entropy rate does not measure the information contained in a random variable.
If we only have a random variable, we only have a shot. In order to consider a \textbf{rate}, we consider a \textbf{random process}: in this case, we have a random variable every certain amount of time: we can say that we have a certain amount of information per second!

A binary random process with $p=0.5$ which gives a value every second, gives 1 bit/s.
So the rate does not measure the information contained in a random variable.
We also need to keep in mind that the differential entropy can be negative.

\begin{theorem}
    A \textbf{Gaussian distribution maximizes the differential entropy}, for a fixed variance.
\end{theorem}

The differential entropy of a $b$ bit quantization of a random variable $x$ is $h(x)+b$.
(what happens if we quantize more and more finely?)

If we discretize a variable in $b$ bits, then what we obtain is another random variable. What if we want to compute the differential entropy of that random variable?
Why do we use differential entropy even though we discretized? We just elongate it:

\begin{center}
\begin{minipage}{0.45\textwidth}
\begin{tikzpicture}[>=Stealth, thick, scale=0.8]
    % 1. Definizione della funzione matematica (Simil-Gaussiana)
    \def\func(#1){1.0 + 3.0*exp(-0.15*(#1-3.5)^2)}

    % 2. Assi Cartesiani
    \draw[->] (-0.5,0) -- (8,0); % Asse X
    \draw[->] (0,-0.5) -- (0,5); % Asse Y

    % 3. Somme di Riemann
    \foreach \x in {0.5, 1.0, 1.5, 2.0, 2.5, 3.0} {
        \pgfmathsetmacro{\height}{\func(\x)}
        \draw[fill=white] (\x,0) rectangle (\x+0.5, \height);
    }

    % 4. La curva continua
    \draw[line width=1.5pt] plot[domain=0:7, samples=100, smooth] (\x, {\func(\x)});

    % 5. Annotazioni
    \draw[blue, line width=1.2pt, rotate around={15:(2.2, 3.0)}] 
        (2.2, 3.0) ellipse (2.5cm and 1.3cm);
    \draw[->, blue, line width=1.2pt] (4.0, 4.5) to[out=30, in=180] (5.2, 4.8);
    \node[blue, right] at (5.2, 4.8) {\large \textsf{Continuous values}};
\end{tikzpicture}
\end{minipage}
\hfill
\begin{minipage}{0.45\textwidth}
    If we push $b \to \infty$, it diverges!\\
    Btw if we want a reference for information theory, we can use the book "Cover \& Thomas" (p. 247).
\end{minipage}
\end{center}

\vspace{1em}
\textsf{Pag 10 of the slides: how to calculate the differential entropy of a Gaussian complex vector.}

\begin{greybox}[Example 1.6: Differential entropy of a complex Gaussian vector]
Let $\bm{x} \sim \mathcal{N}_{\mathcal{C}}(\bm{\mu}, \bm{R})$. From (C.15) and (1.22).
\begin{flalign*}
    h(\bm{x}) &= -\mathbb{E}[\log_2 f_{\bm{x}}(\bm{x})] & (1.23) \\
    &= \log_2 \det(\pi \bm{R}) + \mathbb{E}[(\bm{x} - \bm{\mu})^* \bm{R}^{-1} (\bm{x} - \bm{\mu})] \log_2 e & (1.24) \\
    &= \log_2 \det(\pi \bm{R}) + \text{tr}(\mathbb{E}[(\bm{x} - \bm{\mu})^* \bm{R}^{-1} (\bm{x} - \bm{\mu})]) \log_2 e & (1.25) \\
    &= \log_2 \det(\pi \bm{R}) + \text{tr}(\mathbb{E}[\bm{R}^{-1}(\bm{x} - \bm{\mu})(\bm{x} - \bm{\mu})^*]) \log_2 e & (1.26) \\
    &= \log_2 \det(\pi \bm{R}) + \text{tr}(\bm{R}^{-1}\underbrace{\mathbb{E}[(\bm{x} - \bm{\mu})(\bm{x} - \bm{\mu})^*]}_{\bm{R}}) \log_2 e & (1.27) \\
    &= \log_2 \det(\pi \bm{R}) + \text{tr}(\bm{I}) \log_2 e & (1.28) \\
    &= \log_2 \det(\pi e \bm{R}), & (1.29)
\end{flalign*}
Where in (1.25) we used the fact that a \underline{scalar equals its trace}, while in (1.26) we invoked the commutative property in (B.26).
\tcblower
\textbf{Note:} $\det(\exp(\bm{A})) = \exp(\text{tr}(\bm{A}))$
\end{greybox}

\begin{greybox}[Example 1.5: Differential entropy of a complex Gaussian scalar]
Let $x \sim \mathcal{N}_{\mathcal{C}}(\mu, \sigma^2)$. Invoking the PDF in (C.14),
\[
h(x) = \mathbb{E}\left[ \frac{|x-\mu|^2}{\sigma^2} \log_2 e + \log_2 (\pi \sigma^2) \right]
= \log_2 (\pi e \sigma^2).
\]
\textbf{Equivalent scalar result}
\end{greybox}

Professor freaks out over the fact that we don't know what the trace function is.
The trace of a matrix is the sum of the diagonals.
If we want to compute the sum of the eigenvalues, we can just compute the trace of a matrix.

So we have that:
\begin{align*}
    h(\underline{x}) &= \log_2 (\det (\pi e \underline{R})) \\
    h(x) &= \log_2 (\pi e \sigma^2)
\end{align*}

\textit{At least not for a Gaussian R.V.!}

The differential entropy DOES NOT depend on the mean of the variable!
Another very important thing is that the differential entropy is a property of the distribution of a random variable, not of the random variable itself.
Two random variables having the same distribution will have the same random variable.

\begin{center}
    \begin{tikzpicture}
        \node (text) at (0,0) {\Large $R.V.$ is a map};
        \node (omega) at (2.5,0) {\Large $\Omega$};
        \draw[->, thick] (3.0,0) -- (4.5,0);
        \node (S) at (5.0,0) {\Large $\mathcal{S}$};
        
        \draw[->, blue!70] (omega.south) -- ++(0,-0.5) node[below] {\small \textit{events}};
        \draw[->, blue!70] (S.south) -- ++(0,-0.5) node[below] {\small \textit{probability}};
    \end{tikzpicture}
\end{center}

Elaborating a bit on the computation of the differential entropy: we have that
\begin{align*}
    h(\bm{x}) &= -\mathbb{E}[\log_2 f_{\bm{X}}(\bm{x})] \tag{1.23} \\
    &= -\mathbb{E}\left[ \log_2 \left( \frac{1}{\det(\pi \bm{R}_x)} \exp(-(\bm{X} - \bm{\mu}_x)^* \bm{R}_x^{-1} (\bm{X} - \bm{\mu}_x)) \right) \right] \\
    &= \log_2(\det(\pi \bm{R}_x)) - \mathbb{E}\left[ \frac{\ln(\exp(-(\bm{X} - \bm{\mu}_x)^* \bm{R}_x^{-1} (\bm{X} - \bm{\mu}_x)))}{\ln 2} \right] \\
    &= \log_2(\det(\pi \bm{R}_x)) + \mathbb{E}\left[ (\bm{X} - \bm{\mu}_x)^* \bm{R}_x^{-1} (\bm{X} - \bm{\mu}_x) \right] \log_2 e \tag{1.24}
\end{align*}
and now we observe that $(\bm{X} - \bm{\mu}_x)^* \bm{R}_x^{-1} (\bm{X} - \bm{\mu}_x)$ actually is a scalar. We can then exploit the fact that a scalar is equal to its trace:

\begin{align}
    &= \log_2(\det(\pi \bm{R}_x)) + \text{Tr}\left[ \mathbb{E}\left[ (\bm{X} - \bm{\mu}_x)^* \bm{R}_x^{-1} (\bm{X} - \bm{\mu}_x) \right] \right] \log_2 e \tag{1.25} \\
    &= \log_2(\det(\pi \bm{R}_x)) + \mathbb{E}\left[ \text{Tr}\left[ (\bm{X} - \bm{\mu}_x)^* \bm{R}_x^{-1} (\bm{X} - \bm{\mu}_x) \right] \right] \log_2 e \qquad \text{Linearity} \nonumber \\
    &= \log_2(\det(\pi \bm{R}_x)) + \mathbb{E}\left[ \text{Tr}\left[ \bm{R}_x^{-1} (\bm{X} - \bm{\mu}_x)(\bm{X} - \bm{\mu}_x)^* \right] \right] \log_2 e \qquad \text{Ciclicity} \nonumber \\
    &= \log_2(\det(\pi \bm{R}_x)) + \text{Tr}\left[ \bm{R}_x^{-1} \bm{R}_x \right] \log_2 e \nonumber \\
    &= \log_2(\det(\pi \bm{R}_x)) + \text{Tr}\left[ \bm{I} \right] \log_2 e \tag{1.28}
\end{align}
if then we consider $\bm{R}_x, \bm{I}$ as $N \times N$ matrices, we'll have
\begin{align*}
    &= \log_2(\det(\pi \bm{R}_x)) + N \log_2 e \\
    &= \log_2(e^N \det(\pi \bm{R}_x))
\end{align*}
and, finally, for a matrix $\bm{A}$ of dimensionality $N \times N$ it holds that $a^N \det(\bm{A}) = \det(a\bm{A})$, so:
\begin{equation}
    h(\bm{x}) = \log_2(\det(\pi e \bm{R}_x)). \tag{1.29}
\end{equation}

Finally, we can simplify this for the scalar case as the determinant of a scalar is the scalar itself, getting that:
\[ h(x) = \log_2(\pi e \sigma^2). \]

\begin{greenbox}
    \textbf{\faIcon{pen} Note:} Btw the B.26 relation that they mentioned is
    \begin{equation}
        \text{tr}(\bm{AB}) = \text{tr}(\bm{BA}), \tag{B.26}
    \end{equation}
    which was here applied to $\bm{A} = (\bm{x} - \bm{\mu})^* \bm{R}^{-1}, \bm{B} = (\bm{x} - \bm{\mu})$.
\end{greenbox}

\section{Information divergence, mutual information}

We have that the Kullback-Leibler divergence is defined as:
\begin{equation}
    \mathcal{D}(p||q) = 
    \begin{cases}
        \sum\limits_{x} p(x) \log_2 \left( \frac{p(x)}{q(x)} \right) & \text{discrete } x \\
        \int p(x) \log_2 \left( \frac{p(x)}{q(x)} \right) & \text{continuous } x
    \end{cases}
    \tag{1.34+1.36}
\end{equation}
this is also called the \textit{relative entropy}. Another way to measure such thing is the Earth-Mover distance.

\begin{center}
\begin{tikzpicture}[node distance=2cm, auto, thick]
    % Nodi
    \node[circle, draw] (S) {S};
    \node[draw, rectangle, minimum width=1.2cm, minimum height=1cm] (Ch) [right of=S] {Ch};
    \node[circle, draw] (R) [right of=Ch] {R};

    % Archi
    \draw[->] (S) -- node[below] {$x$} (Ch);
    \draw[->] (Ch) -- node[below] {$y$} (R);
\end{tikzpicture}
\end{center}

The mutual information quantifies the reduction in uncertainty between two random variables if one of them is observed.

\textbf{Discrete:}
\begin{align*}
    \text{I}(s;y) &= H(s) - H(s|y) = H(y) - H(y|s) \\
    &= D(p_{sy} || p_s p_y) \\
    &= \sum_s \sum_y p_{sy}(s,y) \log_2 \left( \frac{p_{sy}(s,y)}{p_s(s)p_y(y)} \right)
\end{align*}

\textbf{Continuous:}
\begin{align*}
    &= \mathbb{E}[\text{information density}] \equiv \mathbb{E}[i(s;y)] \\
    \text{I}(s;y) &= h(s) - h(s|y) = h(y) - h(y|s) \\
    &= D(f_{sy} || f_s f_y) \\
    &= \iint f_{sy}(s,y) \log_2 \left( \frac{f_{sy}(s,y)}{f_s(s)f_y(y)} \right) \, ds \, dy
\end{align*}

He talked about the \colorbox{cyan!30}{Landau symbols}; he didn't say anything relevant really.

\subsection{Power series expansions}
Useful means to approximate functions especially low or high SNR.

\begin{tcolorbox}[colback=white, colframe=blue, sharp corners]
    \textbf{Zero order expansion about 0}
    \[ f(x) = f(0) + \mathcal{O}(x) \]
\end{tcolorbox}

\begin{tcolorbox}[colback=white, colframe=blue, sharp corners]
    \textbf{First order expansion about 0}
    \[ f(x) = f(0) + f'(0)x + \mathcal{O}(x^2) \]
\end{tcolorbox}

\begin{tcolorbox}[colback=white, colframe=blue, sharp corners]
    \textbf{First order expansion about Infinity}
    \[ f(1/x) = f(0) + \frac{f'(0)}{x} + \mathcal{O}\left(\frac{1}{x^2}\right) \]
\end{tcolorbox}

\begin{center}
    \begin{minipage}{0.8\linewidth} 
        \begin{minted}{mathematica}
In[11]:= Series[f[x], {x, 0, 2}]
Out[11]= f[0] + f'[0]x + 1/2 f''[0]x^2 + O[x]^3
        \end{minted}
    \end{minipage}
\end{center}

\vspace{1em}
\noindent
\textbf{Important! $\rightarrow$ SL. \fbox{16}}

\begin{center}
\begin{tikzpicture}[>=latex, auto, node distance=1.5cm]
    % --- Componenti Principali ---
    % 1. Segnale S
    \node (s) {$s$};
    % 2. Amplificatore (Triangolo Blu)
    \node[isosceles triangle,
          isosceles triangle apex angle=60,
          draw=blue, ultra thick,
          shape border rotate=0,
          right of=s, node distance=1.5cm,
          inner sep=4pt] (amp) {};
    % La rho sotto l'amplificatore
    \node[below=0.1cm of amp, text=blue] {$\rho$};
    % 3. Sommatore
    \node[draw, circle, thick, inner sep=0pt, minimum size=0.6cm, right of=amp, node distance=2cm] (sum) {\large $+$};
    % 4. Equazione Output
    \node[right of=sum, node distance=2cm, anchor=west] (eq_y) {$y = s\sqrt{\rho} + $};
    \node[draw=purple!80!red, inner sep=2pt, rounded corners=3pt, thick, right=0pt of eq_y] (z_box) {$z$};

    % 5. Rumore (Noise) in basso
    \node[below of=sum, node distance=2.5cm, align=center, text width=3cm] (noise_label) {$z =$ gaussian\\noise};

    % --- Collegamenti ---
    \draw[->, thick] (s) -- (amp);
    \draw[->, thick] (amp) -- (sum);
    \draw[->, thick] (sum) -- (eq_y);
    \draw[->, thick] (noise_label.north) -- (sum.south);

    % --- Annotazioni ---
    \node[above right=0.3cm and 0.1cm of amp, text=blue] (amp_text) {Amplifier};
    \draw[->, draw=blue, thick] (amp.north) to[out=90, in=180] (amp_text.west);
    
    \node[below=0.8cm of sum, text=purple!80!red, align=center, xshift=1cm] (amp_sig_text) {Amplified\\signal};
    \draw[->, purple!80!red, thick] (2.8, -0.2) -- (amp_sig_text.north); % Approx pos
\end{tikzpicture}
\end{center}

We can ask ourselves: \colorbox{yellow!60}{how much is the gaussian noise corrupting what I'm sending?}

We amplify $\rho$ (which is $\sqrt{\rho}$ in power) $\Rightarrow$ we get an ampl. of $\sqrt{\rho}$ on power!

\subsection{Mutual information of a Gaussian scalar through an AWGN channel}
We were calculating the \textbf{mutual information} for a Gaussian complex scalar.

We talked about the relation $y = \sqrt{\rho}s + z$. We have that $s, z$ are \textbf{independent}.
It might seem weird that we're putting noise in a channel, but it will make sense later.

Our constellation input always will have $\sigma^2 = 1$, such that $s \sim \mathcal{N}_{\mathcal{C}}(0, 1)$; we'll also have a noise $z \sim \mathcal{N}_{\mathcal{C}}(0, 1)$. We have that $y$ is made of a sum of Gaussian independent random variables, and will thus be a Gaussian random variable, with a variance given by a sum of variances: we'll have

\begin{itemize}
    \item $y \sim \mathcal{N}_{\mathcal{C}}(0, 1 + \rho)$
    \item $y|s \sim \mathcal{N}_{\mathcal{C}}(\sqrt{\rho}s, 1)$: this is $y$ with $s$ being fixed. We now want to calculate the mutual information between the input and the output of the signal.
\end{itemize}

We know the differential entropy of a Gaussian scalar and we know that:
\begin{flalign}
    \mathcal{I}(\rho) &= I(s; \sqrt{\rho}s + z) \nonumber \\
    &= h(\sqrt{\rho}s + z) - h(\sqrt{\rho}s + z | s) \nonumber \\
    &= h(\sqrt{\rho}s + z) - h(z) \nonumber \\
    &= \log_2(\pi e (1 + \rho)) - \log_2(\pi e) \nonumber \\
    &= \log_2(1 + \rho). \tag{1.51}
\end{flalign}

\textbf{Relevant comments:} In $h(\sqrt{\rho}s + z|s) = h(z)$, we know that we've already \textbf{fixed $s$}; $s$ multiplied by whichever scalar won't bring any extra information than $z$, as we already know what $s$ is; for this reason, the differential entropies are the same.

\textbf{If we then consider $\rho \to 0$}, we have:
\[ \log_e(1+x) \xrightarrow{x \to 0} x - \frac{1}{2} x^2 + \mathcal{O}(x^3) \]
So we need to \textbf{convert to base $e$} and we'll then be able to use the expansion:
\[ \log_2(1 + \rho) = \left(\rho - \frac{1}{2} \rho^2\right) \log_2 e + \mathcal{O}(\rho^3) \]
And \textbf{for large $\rho$} we'll then have:
\[ \log_2(1 + \rho) \xrightarrow{\rho \to \infty} \log_2(\rho) + \mathcal{O}(1/\rho) \]

\begin{bluebox}
    \textbf{\faIcon{pen} Note:}
    This is \textbf{crucially important} as it allows to model an AWGN channel! It allows us to define the \textbf{capacity} as
    \[ \frac{1}{2} \log(1 + \text{SNR}) \]
    (we don't have $1/2$ since this is complex; we'd have $1/2$ if it wasn't complex!).
\end{bluebox}

\end{document}