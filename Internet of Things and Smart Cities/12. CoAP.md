## Executive Summary

The Constrained Application Protocol (CoAP) is an application layer protocol specifically engineered for the Internet of Things (IoT). It is designed to enable small, low-power devices with limited computational resources and bandwidth to participate in the broader internet. Operating on a client-server paradigm similar to HTTP, CoAP utilizes UDP for transport, necessitating its own mechanisms for error recovery. Security is managed at the network layer via IPsec or through Datagram Transport Layer Security (DTLS).

Key features of CoAP include its compact message format, interoperability with HTTP via gateways, and a unique "resource observation" function that emulates a publish-subscribe model, allowing clients to receive asynchronous updates on a resource's state. The protocol distinguishes between `Message ID`s, used for duplicate detection and acknowledging messages, and `Token`s, used to match specific requests with their corresponding responses. While its RESTful model and simplicity are advantageous for web developers, its fundamental client-server nature makes MQTT a more suitable choice for applications requiring a true publish-subscribe architecture.

--------------------------------------------------------------------------------

## 1. Core Concepts and Architecture

### 1.1. Protocol Purpose and Design

CoAP is an IoT-specific application protocol created to accommodate the limitations of constrained devices, such as scarce bandwidth, low power, limited computational capacity, and long sleep times. Its primary goal is to allow these devices to integrate with the wider internet.

- **Transport Protocol:** CoAP is layered on top of the User Datagram Protocol (UDP), in contrast to protocols like MQTT which typically use TCP. This choice necessitates dedicated error recovery mechanisms within CoAP itself.
- **Security:** To provide security, CoAP can utilize IPsec at the network layer or Datagram Transport Layer Security (DTLS).
- **Communication Model:** It is implemented based on the client-server paradigm, with communication consisting of a sequence of requests (methods) and responses, conceptually similar to HTTP. Although not equivalent to HTTP, internetworking between the two protocols is designed to be straightforward.

### 1.2. System Architecture

CoAP supports several communication patterns:

- Endpoints (clients) can communicate with a CoAP server.
- Endpoints can communicate with an HTTP server through a gateway.
- Endpoints can communicate directly in a peer-to-peer fashion.

A significant feature is that the "lightweight" characteristics of the protocol make it feasible to configure a CoAP server even on resource-constrained endpoints. Furthermore, a device acting as a CoAP server may also act as a client.

## 2. CoAP Frame Structure and Semantics

The CoAP message format is designed to be more compact than HTTP's, though it can be slightly more complex than MQTT's.

**Header Format:**

`V (2 bits) | T (2 bits) | TKL (4 bits) | Code (1 byte) | Message ID (2 bytes)` `Token (0-8 bytes)` `Options (N bytes)` `Payload (K bytes)`

### 2.1. Header Fields Explained

- **Version (V):** 2 bits indicating the CoAP version.
- **Type (T):** 2 bits defining the message type.
    - **0 - Confirmable (CON):** This message requires an acknowledgement message.
    - **1 - Non-confirmable (NON):** This message does not require confirmation.
    - **2 - Acknowledgement (ACK):** A response that acknowledges a Confirmable message.
    - **3 - Reset (RST):** Indicates the receiver cannot or will not process a request.
- **Token Length (TKL):** 4 bits specifying the length of the Token field, which can be 0.
- **Code:** 1 byte indicating the request method or response code. It consists of a 3-bit class and a 5-bit code.
- **Message ID:** A 2-byte value used to detect duplicate messages and retransmissions.
- **Token:** A variable-length field (0-8 bytes) used to correlate a request with its response.

### 2.2. Distinguishing Message ID vs. Token

CoAP uses two separate identifiers for message correlation, each with a distinct purpose.

- **Message ID:** This unique value is used to match a **Confirmable (CON)** message to its corresponding **Acknowledgement (ACK)** message. Its primary role is in ensuring message delivery and detecting duplicates.
- **Token:** This unique value is used to match a **Request** to its **Response**. This is necessary because a response may be sent in a different message than the ACK.

#### Response Delivery Mechanisms

1. **Piggybacked Response:** The response is sent directly within the ACK message for a Confirmable request. In this case, the message has the same Message ID and Token as the original request. This is efficient as it does not require a separate ACK from the client.
2. **Separate Response:** The server first sends an empty ACK to confirm receipt of the request and later sends the full response in a separate message. This is useful if generating the response takes time. The separate response will have the same Token as the request but a different Message ID, and as a new Confirmable message, it will require its own ACK.

The Token may be omitted in Non-confirmable mode or if there is only a single pending request between endpoints where no ambiguity exists.

## 3. Communication: Requests and Responses

### 3.1. Request Methods

CoAP defines several request methods, identified by a code, that are analogous to those in HTTP.

|   |   |
|---|---|
|Method/Request (Code)|Action|
|**GET** (0.01)|Asks for the state of a resource.|
|**POST** (0.02)|Requests the processing of the message content, potentially creating or updating a resource.|
|**PUT** (0.03)|Substitutes the resource specified in the URI or creates a new one if it does not exist.|
|**DELETE** (0.04)|Removes the resource.|

### 3.2. Response Codes

Responses are characterized by codes that indicate the outcome of a request.

|   |   |   |
|---|---|---|
|Response Class|Code|Action|
|**SUCCESS (2.xx)**|||
||`2.01`|**Created**: The resource has been successfully created.|
||`2.02`|**Deleted**: The resource has been successfully deleted.|
||`2.05`|**Content**: The message carries the content (payload) of the requested resource.|
|**CLIENT ERROR (4.xx)**|||
||`4.01`|**Unauthorized**: The client is not authorized to perform the requested action.|
||`4.13`|**Request Entity Too Large**: The size of the client's request exceeds the server's limit.|
|**SERVER ERROR (5.xx)**|||
||`5.01`|**Not Implemented**: The server cannot fulfill the request (e.g., does not recognize the method).|

## 4. Key Functionality: Resource Observation

A powerful feature of CoAP is **resource observation**, which allows a device to be asynchronously updated on the state of a resource, similar to a publish-subscribe model. This is particularly useful in IoT for unsolicited sensory measurements.

- **Initiating Observation:** A client sends a `GET` message with the "Observer" option set to `0`.
- **Receiving Updates:** The "observer" client will then receive a sequence of responses from the server. All these update messages will carry the same Token as the original request. Each response includes a sequence number that increases with each update, allowing the client to reorder messages if they arrive out of order.
- **Update Rate:** The rate of updates depends on the nature of the resource. For example, a temperature sensor resource defined with a 1°C resolution will generate fewer updates than one defined with a 0.1°C resolution for the same physical sensor. Rate-limiting mechanisms can be implemented to prevent message flooding.
- **Terminating Observation:** The observation ends when the server sends a `Reset` message or when the client sends a new `GET` message for the same resource with the "Observer" option set to `1`.

## 5. Final Considerations and Comparison with MQTT

### 5.1. Advantages and Drawbacks

CoAP offers several advantages that make it a strong competitor to MQTT:

- **Simple Design:** Well-suited for IoT networks with many resource-constrained nodes.
- **Asynchronous Support:** The "Observer" method supports asynchronous events where strict real-time guarantees are not required.
- **HTTP Alignment:** Interoperability with HTTP and its REST model makes it easier for web designers to build CoAP applications.

However, it also has drawbacks:

- **Client-Server Model:** Despite the "Observer" feature, CoAP remains a client-server protocol. For applications that require a true, robust publish-subscribe paradigm, MQTT is generally more convenient.

### 5.2. CoAP vs. MQTT Comparison

The following table provides a direct comparison of key features between MQTT and CoAP.

|   |   |   |
|---|---|---|
|Feature|MQTT|CoAP|
|**Purpose**|Messaging and communication in IoT|Designed for resource-constrained devices in IoT|
|**Transport Protocol**|TCP (Transmission Control Protocol)|UDP (User Datagram Protocol)|
|**Communication Style**|Publish/Subscribe model|Request/Response (Client-Server-like) model|
|**Header Size**|2 bytes fixed header|4 bytes fixed header|
|**Payload Format**|Supports binary and text payloads|Supports binary and text payloads|
|**QoS**|Levels 0, 1, and 2 for message delivery|Reliability through "confirmable" and "non-confirmable" messages|
|**Message Types**|Publish, Subscribe, Connect, etc.|GET, POST, PUT, DELETE, etc.|
|**Resource Discovery**|Not inherent, requires extra mechanisms|Built-in resource discovery through CoRE Link Format|